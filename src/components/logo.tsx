import { Link } from "@tanstack/react-router";

const Logo = () => {
  return (
    <Link to="/" className="text-xl md:text-2xl font-bold">
      SUPERSHEYKH
    </Link>
  );
};

export default Logo;
